disabled_rules:
  - npm-package-scripts
  - npm-native-bindings

rules:
  - id: company-custom-mcp
    name: Company custom MCP configuration
    paths:
      - "(^|/)custom-mcp\\.json$"
    patterns:
      - command
      - args
      - docker
